import React from 'react';
import { enhanceItemArray } from '../helpers/functions';

describe('Helpers', () => {
  test('Enhance Item Array', () => {
    const emptyArray = [];

    expect(1).toEqual(1);
  });
});
