export const GAME_START = 'GAME_START';
export const GAME_QUIT = 'GAME_QUIT';
export const RESTART = 'RESTART';

export const MOVE = 'MOVE';

export default {
  RESTART, GAME_START, GAME_QUIT, MOVE,
};
